[project]
name = "autovmware-kimi-ops-runbook"
version = "0.1.0"
description = "给运维使用的 AutoVMware Kimi macOS VMX 克隆交付包"
readme = "README.md"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
